WebDriverWait(driver,timeout)
driver: 传入WebDriver实例，即我们上例中的driver
timeout: 超时时间，等待的最长时间（同时要考虑隐性等待时间）
返回True或False

presence_of_element_located定位的是图片的左上角为原点，向右向下是增大

想做java后端：

基础部分：
java se、java ee(JSP+servlet)、sql、tomcat、算法与数据结构、计算机网络
进阶：
框架：spring、spring mvc、spring boot、struct、mybatis、hibernate
数据库：MongoDB、Redis
其他软件：nginx
操作系统：linux、
版本控制工具：git
项目管理工具：maven/gradle
一些常用开源工具类：gson、dom4j等等
若是要做java游戏后端就要会网络编程（socket、TCP/UDP）、NIO，若是网页后端就要会前端知识（html+css+js+jq+ajax+bootstrap）

控制反转（Inversion of Control，缩写为IoC），是面向对象编程中的一种设计原则，可以用来减低计算机代码之间的耦合度。